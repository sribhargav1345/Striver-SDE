/*
Select prefix of any length from an array and decrement each element in that prefix length by 1.
Make sure no element becomes negative. Count maximum number of zeros after all possible operations.
*/

#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    ll n;
    cin>>n;

    vector<ll> arr;
    for(int i=0;i<n;i++)
    {
        ll x;
        cin>>x;
    
        arr.push_back(x);
    }
    
    int mini = arr[0];
    for(int i=1;i<n;i++)
    {
        
    }
}